import { color } from './style.js'

export const logo = () => {
	return `${color.primary`AWS`}${color.primary.dim`LESS`}`
}

// export const intro = (command: string) => {
// 	console.log()
// 	p.intro(`${logo()} ${color.dim(command)}`)
// }
